<script id="tmpl-GroupInfoView" type="text/html">
  <div class="GroupInfoView">
    <h1>Group Information</h1>
    <p><a role="button" class="btn btn-default groups-link" href="#!/groups/{{>options.providerId}}" data-providerid="{{>options.providerId}}">{{>~tr('Back to overview')}}</a></p>

    <!-- Users -->
    <div class="panel panel-default users">
      <div class="panel-heading">
        <h1 class="panel-title">{{>~tr("Users")}}</h1>
      </div>
      <div class="panel-body"></div>
    </div>

    <!-- Roles -->
    <div class="panel panel-default roles">
      <div class="panel-heading">
        <h1 class="panel-title">{{>~tr("Roles")}}</h1>
      </div>
      <div class="panel-body">
        <p>Content here...</p>
      </div>
    </div>

  </div>
</script>