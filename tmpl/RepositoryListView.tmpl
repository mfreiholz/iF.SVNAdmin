<script id="tmpl-RepositoryListView" type="text/html">
	<div class="RepositoryListView">

		<div class="subnav">
			<div class="container">

				<ul class="nav nav-pills">
					<!-- Providers as drop down menu -->
					{{if providers.length > 1}}
					<li role="presentation" class="dropdown">
						<a class="dropdown-toggle" data-toggle="dropdown" role="button" expanded="false">
							{{>provider.name}} <span class="caret"></span>
						</a>
						<ul class="dropdown-menu">
							{{for providers ~p=provider}}
							<li role="presentation" class="{{if ~p.id == id}}active{{/if}}">
								<a href="#!/repositories?providerid={{url:id}}">{{>displayname}}</a>
							</li>
							{{/for}}
						</ul>
					</li>
					{{/if}}

					{{if provider.editable}}
					<li><a class="NewRepositoryLink" href="">{{>~tr('Create')}}</a></li>
					{{/if}}
				</ul>

			</div>
		</div>

		<div class="content container">

			<p class="list">
				{{for repositories ~provider=provider tmpl="#tmpl-RepositoryListView-RepositoryListItem"/}}
				{{if repositories.length <= 0}}
				<div class="alert alert-info">{{>~tr('No data yet.')}}</div>
				{{/if}}
			</p>

		</div>

	</div>
</script>

<script id="tmpl-RepositoryListView-RepositoryListItem" type="text/html">
	<div class="RepositoryListView-RepositoryListItem">
		<div class="panel panel-default">
			<div class="panel-body">
				{{if ~provider.editable}}
				<button class="btn btn-danger btn-sm pull-right DeleteRepositoryLink" data-id="{{>id}}">{{>~tr('Delete')}}</button>
				{{/if}}
				<a class="title">{{>displayname}}</a>
				<p class="description">Show description from from $REPO/conf/info.conf here.</p>
			</div>
		</div>
	</div>
</script>

<!---------------------- OLD ------------------------->

<script id="tmpl-RepositoryListView_OLD" type="text/html">
	<div class="RepositoryListView">
		<div class="provider-wrapper"></div>
		<div class="repositorylist-wrapper"></div>
	</div>
</script>

<script id="tmpl-RepositoryListView-Providers_OLD" type="text/html">
	{{if providers.length > 1}}
	<div class="RepositoryListView-Providers">
		<ul class="nav nav-pills">
			{{for providers ~current=current}}
			<li class="provider" data-providerid="{{>id}}">
				<a class="provider-link" data-providerid="{{>id}}" href="#!/repositories?providerid={{url:id}}">{{>id}}</a>
			</li>
			{{/for}}
		</ul>
	</div>
	{{/if}}
</script>

<script id="tmpl-RepositoryListView-RepositoryList_OLD" type="text/html">
	<div class="RepositoryListView-RepositoryList">
		<div class="panel panel-primary">
			<div class="panel-heading">
				<h1 class="panel-title">{{>~tr("Repositories")}}</h1>
			</div>
			<div class="panel-body">
				<div class="table-wrapper"></div>
			</div>
			<div class="panel-footer">
				{{if provider.editable}}
				<button class="btn btn-default add-link" data-providerid="{{>providerId}}">{{>~tr("Add")}}</button>
				{{/if}}
			</div>
		</div>
	</div>
</script>